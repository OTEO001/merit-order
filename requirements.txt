# Core pipeline — deliberately small so the daily run has a tiny failure surface.
requests==2.32.3
pandas==2.2.3
jinja2==3.1.4
plotly==5.24.1
markdown==3.7
python-dateutil==2.9.0

# Tests
pytest==8.3.3

# Optional extras (NOT required for the pipeline to run):
#   anthropic>=0.40   -> enables the LLM briefing-polish layer (ENABLE_LLM_POLISH=1)
#   entsoe-py>=0.6    -> enables the European power module (ENABLE_ENTSOE=1)
